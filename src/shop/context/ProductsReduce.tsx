import type { interfaceAction, interfaceState } from "../types/products"

export const ProductsReduce = (state: interfaceState, action: interfaceAction) => {

    console.log(state)
    console.log(action)




}